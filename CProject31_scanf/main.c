/*
=============================================================================
[C언어 강의 핵심교안]
입출력 함수 - scanf()의 모든 것과 초고급 예외 처리 기법

1. scanf()의 어원과 정의
   - scanf는 "Scan Formatted"의 약어임. 직역하면 "형식화된 입력 스캔기".
   - 역할: 사용자가 키보드로 입력한 문자열 상태의 데이터를 프로그램이
          연산할 수 있는 실제 데이터 형태(정수, 실수, 문자 등)로 변환 및 조립함.
   - 목적: 프로그램이 정적 데이터(예: int a = 10;)에만 의존하지 않고,
          사용자의 입력에 따라 동적으로 반응하는 인터랙티브 환경을 구축하기 위함.

2. 표준 콘솔 입력 함수의 비교 및 특징
   - 입력 요구사항에 따라 적절한 함수를 선택해야 메모리 오염과 오작동을 막을 수 있음.

     ① scanf()
        - 특징: 데이터의 '타입(형식)'을 서식 지정자로 지정하여 파싱함.
        - 구분자: 공백(스페이스), 탭(\t), 개행(엔터, \n)을 기준으로 데이터를 쪼갬.
        - 용도: 공백이 없는 데이터 포맷 파싱, 연속된 숫자나 변수를 입력받을 때 적합.

     ② getchar() / _getch()
        - getchar(): 표준 버퍼형 문자 입력 함수. 엔터를 누를 때까지 입력 버퍼에
                     데이터를 대기시켰다가 딱 1바이트(문자 1개)만 가져옴.
                     입력한 문자가 화면에 표시됨(Echo).
                     주로 버퍼에 남은 잔여 데이터(\n 등)를 소거하는 청소용으로 자주 활용됨.
        - _getch(): 비표준 실시간 입력 함수. (<conio.h> 라이브러리 필요)
                    입력 버퍼를 거치지 않고 키보드를 누르는 순간 즉시 값을 프로그램에 전달함.
                    입력한 문자가 화면에 표시되지 않음(No-Echo).
                    용도: 실시간 키 입력을 처리하는 콘솔 게임 구동, 비밀번호 가림 입력 등.

     ③ fgets()
        - 특징: '한 줄(Line)' 전체를 통째로 읽어오는 표준 함수.
        - 안정성: 기존 gets() 함수는 입력 크기 제한이 없어 버퍼 오버플로우 보안 취약점이
                  존재했기에 표준에서 퇴출됨. 대신 크기 제한이 가능한 fgets()를 사용해야 함.
        - 용도: 띄어쓰기가 포함된 문장, 공백이 포함된 긴 텍스트 입력 처리 시 필수적임.

3. 하드웨어와 소프트웨어의 가교: 주소 연산자(&)와 입력 버퍼(Standard Input Buffer)
   - `&` (주소 연산자)
     : 변수명만 지정하면 컴파일러는 값을 저장할 대상을 찾지 못함.
       메모리 공간에서 해당 변수가 할당받은 '실제 번지수(주소)'를 가리켜야 데이터 배달이 가능함.
       그 식별자 역할을 하는 기호가 &임.
       (단, 문자열을 저장하는 배열 이름은 그 자체로 주소 상수를 의미하므로 &를 붙이지 않음)
   - 입력 버퍼 (stdin)
     : 키보드로 타이핑한 값은 OS 내부의 임시 대기실인 '입력 버퍼'에 순차적으로 기록됨.
       scanf는 이 버퍼를 탐색하며 지정된 형식 지정자 규칙에 맞게 데이터를 소모함.
       파싱 과정에서 소비되지 않고 남은 개행 문자(\n)나 비정상 입력값은 버퍼에 그대로
       잔류하며, 이는 후속 입력 함수의 오작동을 유발하는 원인이 됨.

4. 서식 지정자(% 키워드) 가이드 및 고급 제어 기술

    [표준 형식 지정자 요약]
    +--------+------------------+-----------------------------------------------------+
    | 지정자 | 대상 자료형      | 입출력 메커니즘 상의 주의 포인트                    |
    +--------+------------------+-----------------------------------------------------+
    |  %d    | int (정수)       | 선행 공백과 엔터를 자동으로 건너뛰고 숫자만 파싱.   |
    |  %f    | float (실수)     | 단정밀도 실수 입력.                                 |
    |  %lf   | double (실수)    | printf와 달리 scanf에서는 반드시 %lf를 사용해야 함. |
    |  %c    | char (문자)      | 버퍼의 선행 공백, 스페이스, 엔터(\n)까지 문자로     |
    |        |                  | 인식하여 소모하므로 전처리(공백 삽입)가 필수적임.   |
    |  %s    | char[] (문자열)  | 공백이나 엔터를 만나는 즉시 문자열의 끝으로 판단함. |
    +--------+------------------+-----------------------------------------------------+

    [고급 입력 제어 기술]
    - `%[숫자]d / %[숫자]s` : 정해진 바이트 크기만큼만 입력을 강제 제한하여 읽기 처리.
    - `%*d / %*c`         : 스트림 데이터를 읽어들이되, 변수에 저장하지 않고 소거(Filtering).
    - `%[스캔셋]`         : 대괄호 내부에 명시된 문자 집합만 유효한 입력으로 판정함.
    - `%[^스캔셋]`        : 해당 문자가 등장하기 전까지의 모든 텍스트를 스트림에서 추출함.
                            (치트키: %[^\n] -> 개행 문자가 나오기 전까지 공백 포함 전체 흡수)
    - `%i`                : 입력된 데이터의 접두사(0, 0x)를 인식하여 8진수/16진수를 10진수로 자동 변환.

5. 입력 스트림의 종단 처리: EOF(End Of File)와 OS별 입력 단축키
   - 정의: EOF는 데이터 스트림의 끝을 나타내는 표준 상수 값(-1로 정의됨)임.
   - 활용: 데이터의 입력 횟수가 사전에 지정되지 않은 알고리즘이나 대용량 텍스트 파싱 시,
          데이터 유입이 종료되었음을 알리는 신호로 사용됨.
   - 운영체제(OS)별 콘솔 종료 인터럽트 입력 단축키:
      ① Windows 환경
         - 단축키: `Ctrl + Z` 호출 후 `Enter` 입력
         - 동작: 스트림 상에 EOF 신호를 강제로 주입하여 scanf 인터러프트를 빠져나감.
      ② Linux / macOS 환경
         - 단축키: `Ctrl + D` 입력 (별도의 Enter 입력 불요)
         - 동작: 현재 입력 스트림을 즉시 플러시(Flush)하며 데이터가 없으면 EOF로 처리함.
=============================================================================
*/

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main() 
{
    // -------------------------------------------------------------
    // [비밀 요원 프로필 변수 선언]
    // -------------------------------------------------------------
    int agent_id;
    int birth_year, birth_month, birth_day;
    float agility;      // float형 민첩성 스탯
    double weight;      // double형 몸무게 데이터
    char combat_grade;  // 전투 등급 (A, B, C...)
    char codename[20];
    char mission_statement[100];
    int result;

    // [고급 제어 실험용 변수]
    char encrypted_key[5];
    int secret_frequency;
    int training_score;
    int target_floor, target_room;
    int loop_num;       // STEP 11 무한 루프 검증용 변수 (누락 복구 완료)

    printf("================================================================\n");
    printf("         [SECRET AGENT] HQ 국가 기밀 요원 등록 시스템          \n");
    printf("================================================================\n");
    printf(" 시스템 프롬프트에 따라 정보를 입력하십시오. 각 단계는 즉시 검증됩니다.\n\n");


    /*--------------------------------------------------------------
     * 1. 가장 기초적인 입력과 주소 연산자 (&)
     *--------------------------------------------------------------
     * [설명] 입력받은 정수를 변수의 메모리 실제 번지수(&)에 배달함.
     */
    printf("▶ STEP 01. 고유 요원 ID(정수)를 입력하십시오: ");
    scanf("%d", &agent_id);
    printf("  >> [시스템 확인] 요원 고유 번호가 [%d](으)로 메모리에 박제됨.\n\n", agent_id);


    /*--------------------------------------------------------------
     * 2. double형의 정밀도 차이 (%f vs %lf)
     *--------------------------------------------------------------
     * [설명] printf와 달리 scanf에서 double은 무조건 %lf를 써야 정확한 크기로 들어감.
     */
    printf("▶ STEP 02. 요원의 [민첩성 스탯(float)]과 [몸무게(double)]를 입력하십시오.\n");
    printf("   (예시: 98.5 74.25 -> 공백으로 구분): ");
    scanf("%f %lf", &agility, &weight);
    printf("  >> [시스템 확인] 민첩성 스탯: %.1f | 몸무게: %.2lfkg 데이터 할당 완료.\n\n", agility, weight);


    /*--------------------------------------------------------------
     * 3. %c와 입력 버퍼(Buffer)의 유령 엔터 스킵
     *--------------------------------------------------------------
     * [설명] 앞서 엔터친 기호(\\n)가 버퍼에 남아 %c를 오염시키는 것을 막기 위해 " %c" 공백 사용.
     */
    printf("▶ STEP 03. 초기 전투 등급을 입력하십시오 (A, B, C, D 중 택1): ");
    scanf(" %c", &combat_grade);
    printf("  >> [시스템 확인] 당신의 최종 가드 등급은 [%c] 등급으로 판정됨.\n\n", combat_grade);


    /*--------------------------------------------------------------
     * 4. % 키워드 ① : 특정 길이만큼 잘라 읽기 (%[숫자]d)
     *--------------------------------------------------------------
     * [설명] 붙여 써도 지정된 고정 길이 바이트만큼 칼같이 데이터를 파싱함.
     */
    printf("▶ STEP 04. 생년월일 8자리를 공백 없이 붙여서 입력하십시오 (예: 20050815): ");
    scanf("%4d%2d%2d", &birth_year, &birth_month, &birth_day);
    printf("  >> [시스템 확인] 파싱 결과: %d년 %d월 %d일 출생 요원 확인.\n\n", birth_year, birth_month, birth_day);


    /*--------------------------------------------------------------
     * 5. % 키워드 ② : 읽기 거부와 데이터 필터링 (%*c)
     *--------------------------------------------------------------
     * [설명] 서식 중간에 %*c를 사용하면 해당 위치의 숫자는 스캔은 하되 변수에 저장하지 않고 버림.
     */
    printf("▶ STEP 05. 타겟 건물의 [층수]와 [방 번호]를 대시(-) 기호와 함께 입력하십시오.\n");
    printf("   (예시: 12-405 입력 시 중간의 마라톤 기호 '-'는 무시됨): ");
    // [12] 저장, [-]는 읽고 무시(%*c), [405] 저장
    scanf_s("%d%*c%d", &target_floor, &target_room);

    printf("  >> [시스템 확인] 작전 투입 공간 확정 -> [%d층 %d호]\n\n", target_floor, target_room);


    /*--------------------------------------------------------------
     * 6. %s의 공백 한계점 테스트
     *--------------------------------------------------------------
     * [설명] 배열명 자체가 주소이므로 &가 없으며, 공백이나 엔터를 만나면 입력을 종료함.
     */
    printf("▶ STEP 06. 요원의 비밀 코드네임을 입력하십시오 (공백 금지): ");
    scanf("%s", codename);
    printf("  >> [시스템 확인] 임무 수행 코드네임: 명칭 [%s] 활성화.\n\n", codename);


    /*--------------------------------------------------------------
     * 7. % 키워드 ③ : 스캔셋(Scanset)으로 한 줄 전체 흡수 (%[^문자])
     *--------------------------------------------------------------
     * [설명] %[^\n] 서식은 줄바꿈 엔터 문자가 오기 전까지의 모든 공백 문장을 통째로 가져옴.
     */
    printf("▶ STEP 07. 사령부 전송용 각오 한마디를 입력하십시오 (공백/띄어쓰기 가능): ");
    scanf(" %[^\n]", mission_statement);
    printf("  >> [시스템 확인] 전송된 문장: \"%s\"\n\n", mission_statement);


    /*--------------------------------------------------------------
     * 8. 심화 ① : 버퍼 오버플로우 방지벽 구축 (%[숫자]s)
     *--------------------------------------------------------------
     * [설명] 크기가 5인 배열에는 최대 4글자만 넣어야 함. %4s로 강제 차단벽을 세움.
     */
    printf("▶ STEP 08. [보안 실험] 4글자 제한 메인 프레임 암호키를 무작위로 길게 입력하십시오.\n");
    printf("   (아무리 길게 쳐도 시스템은 정확히 4글자만 안전하게 잘라냅니다): ");
    scanf("%4s", encrypted_key);
    printf("  >> [시스템 확인] 버퍼 컷오프 완료. 승인된 암호화 키 코드: [%s]\n\n", encrypted_key);


    /*--------------------------------------------------------------
     * 9. 심화 ② : 진법 카멜레온 자동 분석기 (%i)
     *--------------------------------------------------------------
     * [설명] %i는 입력된 데이터의 접두사(0, 0x)를 기반으로 8진수, 16진수를 자동 인지함.
     */
    printf("▶ STEP 09. 통신 주파수를 입력하십시오. 진법 규칙이 자동 적용됩니다.\n");
    printf("   - 일반 입력 (예: 16)   -> 10진수로 인지\n");
    printf("   - 0으로 시작 (예: 016) -> 8진수로 인지\n");
    printf("   - 0x로 시작 (예: 0x16) -> 16진수로 인지\n");
    printf("   주파수 번호 입력: ");
    scanf("%i", &secret_frequency);
    printf("  >> [시스템 확인] 자동 해독 결과 (10진수 변환값): [%d MHz]\n\n", secret_frequency);


    /*--------------------------------------------------------------
     * 10. 방어 코딩의 핵심: scanf의 리턴값 검증과 에러 버퍼 청소
     *--------------------------------------------------------------
     * [설명] scanf는 정상 수집한 개수를 반환함. %d에 글자를 치면 0을 리턴하므로 버퍼를 비워야 함.
     */
    printf("▶ STEP 10. [방어막 테스트] 오늘의 사격 점수(정수)를 입력하십시오.\n");
    printf("   (※ 고의로 문자 'abc' 등을 입력하여 시스템 예외 처리 작동을 확인해 보셈): ");
    result = scanf("%d", &training_score);

    if (result == 0) 
    {
        printf("  >> [★경고] 비정상 입력 스트림 감지! 숫자가 아닙니다.\n");
        printf("  >> [조치] 잔류 쓰레기 데이터를 버퍼에서 강제 소거합니다.\n");
        while (getchar() != '\n'); // 버퍼 청소 완료
        training_score = 0; // 기본값 강제 할당
    }
    else 
    {
        printf("  >> [시스템 확인] 정상 데이터 수집 완료. 훈련 점수: [%d 점]\n\n", training_score);
    }


    /*--------------------------------------------------------------
     * 11. 심화 ③ : 불특정 무한 입력 처리의 마침표 - EOF 판단 루프
     *--------------------------------------------------------------
     * [설명] 개수가 정해지지 않은 스트림 파싱 시, 콘솔 강제 종료 신호(EOF, -1)를 잡아 탈출함.
     */
    printf("\n▶ STEP 11. [최종 모의 훈련] 탄착 지점 거리 데이터를 연속으로 입력하십시오.\n");
    printf("   - 정수를 입력할 때마다 대기 버퍼에서 데이터를 소모하여 실시간으로 출력함.\n");
    printf("   - [훈련 종료 방법]: 숫자를 치고 엔터를 친 뒤, 'Ctrl + Z'를 누르고 엔터를 한 번 더 치십시오 (EOF 주입).\n");

    // 이전 스트림에 걸려있을지 모르는 찌꺼기 문자열 완전 배출
    while (getchar() != '\n');

    int loop_cnt = 1;
    while (scanf("%d", &loop_num) != EOF) 
    {
        printf("      [%d회차 사격 통제실 수신 데이터] => 거리: %d미만 타격 확인.\n", loop_cnt++, loop_num);
    }
    printf("  >> [시스템 확인] 스트림 종단 신호(EOF) 접수. 무한 루프 훈련 세션을 안전하게 종료함.\n");


    // -------------------------------------------------------------
    // [모든 훈련 및 설정 데이터 최종 인쇄 화면]
    // -------------------------------------------------------------
    printf("\n================================================================\n");
    printf(" [PROFILED DATA] 국가 최고 정보국 비밀 요원 명부 등록 완료\n");
    printf("================================================================\n");
    printf(" 요원 식별 번호 : %d\n", agent_id);
    printf(" 신체 프로필   : 민첩도 %.1f / 몸무게 %.2lfkg\n", agility, weight);
    printf(" 전투 가드 등급 : %c 등급\n", combat_grade);
    printf(" 요원 생년월일 : %d년 %d월 %d일\n", birth_year, birth_month, birth_day);
    printf(" 기습 침투 좌표 : 타겟 빌딩 %d층 %d호\n", target_floor, target_room);
    printf(" 공식 코드네임 : %s\n", codename);
    printf(" 전송된 신념   : %s\n", mission_statement);
    printf(" 메인 암호 코드 : %s\n", encrypted_key);
    printf(" 동기화 주파수 : %d MHz\n", secret_frequency);
    printf(" 최종 사격 점수 : %d 점\n", training_score);
    printf("================================================================\n");

    return 0;
}